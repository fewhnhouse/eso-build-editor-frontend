import React from "react";
import styled from "styled-components";

const theme = {
    primary: "blue",
    healthRed: "red",
    stamGreen: "green",
    magBlue: "blue",
    statsHeal: "yellow",
    statsRes: "cyan",
    mainBg: "rgb(155, 155, 155)"
};

export default () => {
    return theme;
}